import styled from "styled-components";
import {Box} from "@mui/material";

export const StyledSeparateLine = styled(Box)`
  && {
    margin-top: 10px;
    height: 1px;
    background-color: #E5E5E5;
  }
`