import styled from "styled-components";
import {Box, Typography} from "@mui/material";

export const StyledSignUpBox = styled(Box)`
  && {
    margin-top: 49px;
  }
`

export const StyledCheckBoxContainer = styled(Box)`
  && {
    margin-top: 32px;
    display: flex;
  }
`

export const StyledConfirmInformation = styled(Typography)`
  && {
    font-size: 14px;
    font-weight: 400;
    line-height: 24px;
    color: #231F20;
  }
`

export const StyledConfirmButtonBox = styled(Box)`
  && {
    margin-top: 40px;
    width: 100%;
  }
`
