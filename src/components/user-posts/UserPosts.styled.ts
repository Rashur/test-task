import styled from "styled-components";
import {Box} from "@mui/material";

export const StyledMainUserPostsBox = styled(Box)`
  && {
    margin-top: 15px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
`