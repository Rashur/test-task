import styled from "styled-components";
import {Container} from "@mui/material";

export const StyledUsersPageContainer = styled(Container)`
  && {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 23px;
  }
`

export const StyledUsersContainer = styled(Container)`
  && {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
  }
`