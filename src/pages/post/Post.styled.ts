import styled from "styled-components";
import {Box, Container, Typography} from "@mui/material";

export const StyledPostContainer = styled(Container)`
  && {
    margin-top: 23px;
  }
`

export const StyledCommentTitle = styled(Typography)`
  && {
    font-size: 20px;
    font-weight: 700;
    line-height: 26px;
    color: #373F41;
  }
`